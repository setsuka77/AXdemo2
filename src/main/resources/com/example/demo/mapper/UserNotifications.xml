<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserNotificationsMapper">

	<!-- お知らせ情報をuserに紐づける -->
	<insert id="insert"
		parameterType="com.example.demo.entity.UserNotifications">
		INSERT INTO user_notifications (user_id, notification_id,  is_read, created_at)
		VALUES (#{userId}, #{notificationId}, #{isRead},#{createdAt})
	</insert>

</mapper>