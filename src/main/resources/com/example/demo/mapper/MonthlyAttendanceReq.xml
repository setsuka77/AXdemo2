<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.MonthlyAttendanceReqMapper">

    <!-- MonthlyAttendanceReqとUsersをマッピング -->
    <resultMap id="MonthlyAttendanceReqWithUserMap"
        type="com.example.demo.entity.MonthlyAttendanceReq">
        <result property="id" column="reqId" />
        <result property="userId" column="userId" />
        <result property="targetYearMonth" column="targetYearMonth" />
        <result property="date" column="reqDate" />
        <result property="status" column="reqStatus" />
        <association property="user"
            javaType="com.example.demo.entity.Users">
            <result property="name" column="name" />
        </association>
    </resultMap>

    <!-- ユーザ情報と月次勤怠申請情報を取得 -->
    <select id="getMonthlyAttendanceReqWithUser"
        resultMap="MonthlyAttendanceReqWithUserMap">
        SELECT
            mar.id as reqId,
            mar.user_id as userId,
            mar.target_year_month as targetYearMonth,
            mar.date as reqDate,
            mar.status as reqStatus,
            u.name as name
        FROM
            monthly_attendance_req mar
        INNER JOIN users u ON mar.user_id = u.id
        WHERE
            mar.status = #{status}
    </select>

</mapper>
