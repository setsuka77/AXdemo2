<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.AttendanceMapper">
   
   <!-- 勤怠登録情報取得(ユーザーID) -->
    <select id="findByUserId" parameterType="int" resultType="com.example.demo.entity.Attendance">
        SELECT * FROM attendance WHERE user_id = #{userId}
    </select>

    <!-- 勤怠登録情報用DTOリスト取得 -->
    <select id="getAttendance" parameterType="map" resultType="com.example.demo.dto.AttendanceDto">
        SELECT * FROM attendance WHERE user_id = #{userId} AND date = #{date}
    </select>

    <!-- 勤怠登録情報登録 -->
    <insert id="insert" parameterType="com.example.demo.entity.Attendance">
        INSERT INTO attendance (user_id, status, date, start_time, end_time, remarks)
        VALUES (#{userId}, #{status}, #{date}, #{startTime}, #{endTime}, #{remarks})
    </insert>

    <!-- 勤怠登録情報 承認申請 -->
    <!--  <update id="approveAttendance" parameterType="map">
      
    </update>
    -->
</mapper>